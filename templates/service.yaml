apiVersion: v1
kind: Service
metadata:
  name: {{ include "ci.fullname" . }}
  labels:
    {{- include "ci.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ci.selectorLabels" . | nindent 4 }}
